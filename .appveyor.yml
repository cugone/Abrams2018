image: Visual Studio 2017
configuration: Release
platform:
  - x64
cache:
  - C:\projects\Abrams2018\Tests\packages -> Tests\packages\packages.config
install:
  - nuget restore
build:
  project: Engine.sln

test:
  assemblies:
    only:
      - $(SolutionDir)Temporary\$(ProjectName)_$(Platform)_$(Configuration)\Tests.exe

before_test:
  - echo -----------------------ABOUT TO RUN TEST.EXE---------------------------------

test_script:
  - '$(SolutionDir)Temporary\$(ProjectName)_$(Platform)_$(Configuration)\Tests.exe'

after_test:
  - echo -----------------------DONE RUNNING TEST.EXE-----------------------------------
